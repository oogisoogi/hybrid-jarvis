# 작업 브리프 — TICKET=<티켓명> (YYYY-MM-DD · 발주 master)

> 저장 위치: <프로젝트>/.briefs/brief-<티켓명>-<날짜>.md
> 워커는 이 브리프만 보고도 완주할 수 있어야 한다(자기완결). 모호하면 착수 전에 질문하라.

## 1. 목표
<이 티켓이 끝나면 세상이 어떻게 달라져 있는가 — 한 문단>

## 2. 이월(캐리오버) — 필수 칸
<직전 라운드·전임 워커에서 넘어온 상태·미결·함정. 없으면 반드시 "이월 없음"이라고 적는다.>

## 3. 비가역·비용 결정 목록 — 필수 칸
<이 티켓 범위에서 예상되는 되돌리기 어려운 결정을 열거. 없으면 "해당 없음".>
| 결정 | 게이트 |
|---|---|
| 예: 원격 DB 마이그레이션 | ⛔ 워커 실행 금지 · master 게이트 |
| 예: 배포 | ⛔ 워커 실행 금지 · master 게이트 |
| 예: 외부 계정 생성·과금 | ⛔ 오너 게이트 |

## 4. 상한·중단 조건 — 필수 칸
- 시간 상한: <예: 3h — 초과 시 중간 상태 커밋 + 【막힘】 push>
- 재시도 상한: <예: 같은 실패 3회면 중단·보고>
- 막힘 보고 조건: <무엇이 안 되면 진행을 멈추고 물어보는가>

## 5. 작업 내용
<구체 작업 목록 — 순서·파일·기준>

## 6. 검증 요구
- 자체검증: <테스트 스위트·게이트 스크립트 — 전건 통과 증거(출력 원문) 요구>
- 이종 검증: <적대검증 지시 여부·통과 기준>
- 완료 보고 서식: 【검수요청】 push + 커밋 해시 + 검증 증거. "될 것"이 아니라 "확인했다"로 쓸 것.

## 7. 경계
- 무접촉: <이 티켓이 건드리면 안 되는 파일·기능>
- 정본 문서: <이 작업의 SOT가 되는 파일>
